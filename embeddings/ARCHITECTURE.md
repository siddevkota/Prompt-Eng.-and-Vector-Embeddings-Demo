# 🏗️ Architecture & Flow Diagrams

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    Streamlit Web Application                 │
│                           (app.py)                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   OpenAI     │    │    FAISS     │    │  LangChain   │
│  Embeddings  │    │Vector Store  │    │   Pipeline   │
│     API      │    │   (Local)    │    │  Framework   │
└──────────────┘    └──────────────┘    └──────────────┘
        │                     │                     │
        │                     │                     │
        ▼                     ▼                     ▼
┌──────────────────────────────────────────────────────────────┐
│                     Visualization Layer                       │
│              (Plotly, scikit-learn, NumPy)                   │
└──────────────────────────────────────────────────────────────┘
```

---

## Data Flow: Embedding Generation

```
Text Input
    │
    ▼
┌─────────────────┐
│  "Hello World"  │  ← User enters text
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ OpenAI API Call │  ← text-embedding-3-small model
└─────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│ Vector: [0.123, -0.456, ..., 0.789] │  ← 1536 dimensions
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────┐
│ Store in Memory │  ← Session state
└─────────────────┘
    │
    ▼
┌─────────────────┐
│   Visualize     │  ← PCA/t-SNE to 2D/3D
└─────────────────┘
```

---

## Vector Search Flow

```
Query: "machine learning"
    │
    ▼
┌─────────────────┐
│ Embed Query     │  ← Convert to vector
└─────────────────┘
    │
    ▼
┌─────────────────┐
│  FAISS Index    │  ← Search similar vectors
│   Search        │
└─────────────────┘
    │
    ▼
┌─────────────────────────┐
│ Calculate Distances     │  ← L2 distance to all vectors
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ Rank by Similarity      │  ← Sort results
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ Return Top K Results    │  ← Show to user
└─────────────────────────┘
```

---

## RAG Pipeline Flow

```
Document: "AI is transforming..."
    │
    ▼
┌─────────────────────────┐
│  Chunk Document         │  ← Split into pieces
│  Size: 200 chars        │
│  Overlap: 50 chars      │
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ Chunk 1: "AI is..."     │
│ Chunk 2: "...machine..."│  ← Multiple chunks
│ Chunk 3: "...learning." │
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ Embed Each Chunk        │  ← OpenAI embeddings
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ Store in FAISS Index    │  ← Vector database
└─────────────────────────┘
    │
    │ ... Later ...
    │
Query: "What is ML?"
    │
    ▼
┌─────────────────────────┐
│ Embed Query             │  ← Convert to vector
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ Retrieve Top K Chunks   │  ← Semantic search
│ (e.g., K=3)             │
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ Chunk 2: "...machine..."│  ← Retrieved context
│ Chunk 1: "AI is..."     │
│ Chunk 5: "...learns..."  │
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ Combine with Query      │  ← Create prompt
│ Context: [chunks]       │
│ Question: "What is ML?" │
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ GPT-3.5-turbo          │  ← Generate answer
└─────────────────────────┘
    │
    ▼
┌─────────────────────────┐
│ Answer: "Machine        │  ← Return to user
│ learning is..."         │
└─────────────────────────┘
```

---

## Embedding Space Visualization

```
High-Dimensional Space (1536D)
    │
    │ Text 1: [0.1, 0.2, ..., 0.5]  ← Original embedding
    │ Text 2: [0.3, 0.1, ..., 0.4]
    │ Text 3: [0.8, 0.9, ..., 0.7]
    │
    ▼
┌─────────────────────────┐
│ Dimensionality          │  ← PCA or t-SNE
│ Reduction               │
└─────────────────────────┘
    │
    ▼
2D/3D Space
    │
    │   Text 2 ●
    │         Text 1 ●    ← Similar texts cluster
    │
    │               ● Text 3  ← Different texts separate
    │
    └─── X axis
```

---

## Component Interactions

```
┌────────────────────────────────────────────────────────┐
│                    Streamlit App                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │            Session State Manager                  │ │
│  │  - embeddings_data                                │ │
│  │  - vectorstore                                    │ │
│  │  - documents                                      │ │
│  └──────────────────────────────────────────────────┘ │
│                                                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │  Tab 1   │  │  Tab 2   │  │  Tab 3   │           │
│  │Embeddings│  │  Search  │  │Visualize │  ...      │
│  └──────────┘  └──────────┘  └──────────┘           │
└────────────────────────────────────────────────────────┘
        │               │               │
        ▼               ▼               ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│ OpenAI   │    │  FAISS   │    │ Plotly   │
│   API    │    │  Index   │    │  Charts  │
└──────────┘    └──────────┘    └──────────┘
```

---

## User Journey Map

```
1. Start
   │
   ▼
2. Enter API Key → Configure
   │
   ▼
3. Tab 1: Generate Embeddings
   │  - Enter text
   │  - Click generate
   │  - View stats
   │
   ▼
4. Tab 3: Visualize (Optional)
   │  - Choose method (PCA/t-SNE)
   │  - See 2D/3D plot
   │  - Explore heatmap
   │
   ▼
5. Tab 2: Search
   │  - Build index
   │  - Enter query
   │  - View results
   │
   ▼
6. Tab 4: RAG Pipeline
   │  - Input document
   │  - Process & chunk
   │  - Ask questions
   │  - Get answers
   │
   ▼
7. Experiment & Learn!
```

---

## Technical Stack

```
┌─────────────────────────────────────┐
│         Frontend (User Interface)    │
│              Streamlit               │
└─────────────────────────────────────┘
               │
┌─────────────────────────────────────┐
│        Application Logic             │
│     Python + Custom Code             │
└─────────────────────────────────────┘
               │
┌──────────────┬──────────────┬────────────────┐
│   ML/AI      │  Data Proc   │  Visualization │
│  - OpenAI    │  - NumPy     │  - Plotly      │
│  - LangChain │  - Pandas    │  - Matplotlib  │
│  - FAISS     │  - sklearn   │                │
└──────────────┴──────────────┴────────────────┘
               │
┌─────────────────────────────────────┐
│         External Services            │
│         OpenAI API (Cloud)           │
└─────────────────────────────────────┘
```

---

## Performance Considerations

```
Bottlenecks:
    │
    ├── API Calls (Slowest)
    │   - Embedding generation: ~100-500ms per call
    │   - Chat completion: ~1-3s per call
    │   - Solution: Batch requests, cache embeddings
    │
    ├── Vector Search (Fast)
    │   - FAISS search: <10ms for small datasets
    │   - Scales well with proper indexing
    │   - Solution: Use IVF indexes for large datasets
    │
    └── Visualization (Medium)
        - PCA: Fast (< 1s for 100s of points)
        - t-SNE: Slower (2-10s depending on size)
        - Solution: Use PCA for large datasets
```

---

## Security & Privacy

```
┌─────────────────────────────────────┐
│         User's Browser               │
│  - API key in session state only     │
│  - Not saved to disk                 │
│  - Cleared when browser closes       │
└─────────────────────────────────────┘
               │ HTTPS
               ▼
┌─────────────────────────────────────┐
│         Local Server                 │
│  - Runs on localhost:8501            │
│  - No data persistence               │
│  - Session isolated                  │
└─────────────────────────────────────┘
               │ HTTPS
               ▼
┌─────────────────────────────────────┐
│         OpenAI API                   │
│  - Encrypted communication           │
│  - API key authentication            │
│  - Zero data retention (ZDR mode)    │
└─────────────────────────────────────┘
```

---

## Scaling Strategies

```
Current Setup (Development)
  ├── Local FAISS index (in-memory)
  ├── Session-based storage
  └── Single user

To Scale Up:
  ├── Persistent Vector DB
  │   ├── Chroma (local with persistence)
  │   ├── Pinecone (cloud-native)
  │   └── Weaviate (self-hosted or cloud)
  │
  ├── Caching Layer
  │   ├── Redis for embeddings
  │   └── LRU cache for queries
  │
  ├── Background Processing
  │   ├── Celery for async tasks
  │   └── Queue for batch embedding
  │
  └── Multi-User Support
      ├── User authentication
      ├── Separate indexes per user
      └── Rate limiting
```

---

## Error Handling Flow

```
User Action
    │
    ▼
Try:
    │
    ├── API Call
    │   ├── Success → Return result
    │   └── Fail → Catch exception
    │
    ├── Processing
    │   ├── Success → Continue
    │   └── Fail → Catch exception
    │
    └── Display
        ├── Success → Show result
        └── Fail → Catch exception
            │
            ▼
        Error Handler
            │
            ├── Check API key
            ├── Check rate limits
            ├── Check data format
            └── Show user-friendly message
```

---

This architecture enables:
- ✅ Modular design
- ✅ Easy testing
- ✅ Scalable components
- ✅ Clear data flow
- ✅ User-friendly interface
